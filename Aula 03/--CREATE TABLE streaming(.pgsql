--CREATE TABLE streaming(
     --id INT GENERATED ALWAYS AS IDENTITY PRIMARY KEY NOT NULL,
     --nome VARCHAR(100) NOT NULL,
     --duracao NUMERIC(10,2) NOT NULL,
     --avaliacao INT NOT NULL DEFAULT 0
--);

SELECT * FROM streaming;

--DROP TABLE produtos;

--INSERT INTO streaming(nome,duracao,avaliacao)
--VALUES('A Guerra das Comidas','91','1');

--DELETE FROM streaming
--WHERE id IN (15,16,17,18,19,20);

--SELECT avaliacao,nome FROM streaming ORDER BY avaliacao DESC;

--UPDATE streaming
--avaliacao=10
--HERE nome='Descendentes';

--DELETE FROM produtos
--WHERE id IN (4,2,5);

--UPDATE streaming
--SET avaliacao=8
--WHERE nome='Girl Meets World';